from trello import TrelloClient

client = TrelloClient(api_key='XXXX',
                      api_secret='XXXX',
                      token='XXX',
                      token_secret='XXXX')

boards = client.list_boards() # Obtiene todos los tableros

# Para obtener los identificadores de los tableros o los nombres
for board in boards:
  board.id
  board.name

# Con esto nos traemos un tablero
myboard = client.get_board('563503b4a0e0741f6f87a9a6')

# Con esto nos traemos las listas de un tablero en una coleccion de objetos
mylists = myboard.open_lists()

# Tambien tenemos cosas como
all_lists() closed_lists() y get_list(list_id)

# Para traernos las tarjetas de una lista.
mylists[0].list_cards()

# Agregar card
added_card = trello_lists[0].add_card('Card agregada desde python')

added_card.id -> Retorna el ID.
